# Project Rules & Customizations

## Git Constraints
- NEVER propose, execute, or stage commands involving `git push`, `git commit`, `git add`, or `git init` unless the user explicitly requests it in the current chat turn.
- Before suggesting or initiating any Git repository updates or command executions, always verify that the user has explicitly typed a request for that operation in their latest message.
